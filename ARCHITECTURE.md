# 🧬 Project Ouroboros - Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         PROJECT OUROBOROS                            │
│                       The Living Software                            │
└─────────────────────────────────────────────────────────────────────┘

┌────────────────────────────┐          ┌──────────────────────────────┐
│    FRONTEND (Vercel)       │          │    BACKEND (Railway)          │
│    Next.js 14 + Tailwind   │          │    Python + FastAPI           │
├────────────────────────────┤          ├──────────────────────────────┤
│                            │          │                              │
│  ┌──────────────────────┐ │          │  ┌────────────────────────┐ │
│  │   God View Dashboard │ │          │  │      main.py           │ │
│  │  ┌────────────────┐  │ │          │  │   (FastAPI Server)     │ │
│  │  │ Vital Signs    │  │ │          │  │                        │ │
│  │  │ • Status       │  │ │          │  │  Endpoints:            │ │
│  │  │ • Generation   │  │ │          │  │  • GET  /status        │ │
│  │  │ • Exec Time    │  │ │          │  │  • GET  /logs          │ │
│  │  └────────────────┘  │ │          │  │  • POST /chaos         │ │
│  │                      │ │          │  │  • GET  /health        │ │
│  │  ┌────────────────┐  │ │          │  │                        │ │
│  │  │ Statistics     │  │ │          │  │  CORS Enabled          │ │
│  │  │ • Success Rate │  │ │          │  └───────┬────────────────┘ │
│  │  │ • Crashes      │  │ │          │          │                  │
│  │  │ • Uptime       │  │ │          │          │                  │
│  │  └────────────────┘  │ │          │  ┌───────▼────────────────┐ │
│  │                      │ │          │  │    watcher.py          │ │
│  │  ┌────────────────┐  │ │          │  │  (Immune System)       │ │
│  │  │ Chaos Controls │  │ │          │  │                        │ │
│  │  │ • Syntax Error │  │ │          │  │  • Runs in thread      │ │
│  │  │ • Div by Zero  │  │ │          │  │  • subprocess.run()    │ │
│  │  │ • Delete Line  │  │ │          │  │  • Captures errors     │ │
│  │  │ • Random       │  │ │          │  │  • Monitors perf       │ │
│  │  └────────────────┘  │ │          │  │  • Triggers mutation   │ │
│  │                      │ │          │  │  • Logs everything     │ │
│  │  ┌────────────────┐  │ │          │  └───────┬────────────────┘ │
│  │  │ Live Terminal  │  │ │          │          │ Spawns            │
│  │  │ (Log Stream)   │  │ │          │          │ subprocess        │
│  │  └────────────────┘  │ │          │  ┌───────▼────────────────┐ │
│  │                      │ │          │  │    organism.py         │ │
│  │  ┌────────────────┐  │ │          │  │   (The Living Code)    │ │
│  │  │ Mutation Hist  │  │ │          │  │                        │ │
│  │  │ (Last change)  │  │ │          │  │  • Performs work       │ │
│  │  └────────────────┘  │ │          │  │  • Bubble sort         │ │
│  └──────────────────────┘ │          │  │  • Prints output       │ │
│                            │          │  │  • Can crash           │ │
│  Polling every 1 second    │          │  │  • Self-modifiable     │ │
│                            │          │  └────────────────────────┘ │
└────────────┬───────────────┘          │                              │
             │                          │  (Writable Filesystem)       │
             │  HTTP/REST               └──────────────────────────────┘
             │  (CORS)
             │
             │  GET /status
             ├──────────────►
             │               { generation, status, logs, ... }
             │◄──────────────
             │
             │  GET /logs
             ├──────────────►
             │               { logs: [...] }
             │◄──────────────
             │
             │  POST /chaos
             ├──────────────►
             │               { message: "chaos injected" }
             │◄──────────────


┌─────────────────────────────────────────────────────────────────────┐
│                        THE MUTATION LOOP                             │
└─────────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────┐
    │                                                    │
    │  1. Watcher spawns organism.py as subprocess      │
    │                                                    │
    │  2. Organism runs → does work → prints output     │
    │                                                    │
    │  3. Organism exits (success or crash)             │
    │                                                    │
    │  4. Watcher captures:                             │
    │     • Return code                                 │
    │     • stdout (normal output)                      │
    │     • stderr (error messages)                     │
    │     • Execution time                              │
    │                                                    │
    │  5. Decision Tree:                                │
    │                                                    │
    │     ┌─────────────────────┐                       │
    │     │ Exit code == 0?     │                       │
    │     └─────────┬───────────┘                       │
    │               │                                    │
    │        ┌──────┴──────┐                            │
    │        │             │                            │
    │      YES            NO                            │
    │        │             │                            │
    │        ▼             ▼                            │
    │    ┌───────┐    ┌──────────────┐                 │
    │    │ Check │    │ MUTATION     │                 │
    │    │ Speed │    │ TRIGGERED!   │                 │
    │    └───┬───┘    │              │                 │
    │        │        │ • Log error  │                 │
    │    Slow?        │ • Call LLM*  │                 │
    │        │        │ • Save v{N}  │                 │
    │       YES       │ • Increment  │                 │
    │        │        │   generation │                 │
    │        ▼        └──────────────┘                 │
    │  ┌──────────┐         │                          │
    │  │ Optimize │         │                          │
    │  │ Mutation │         │                          │
    │  └────┬─────┘         │                          │
    │       │               │                          │
    │       └───────┬───────┘                          │
    │               │                                  │
    │               ▼                                  │
    │      ┌────────────────┐                          │
    │      │ Wait 3 seconds │                          │
    │      └────────┬───────┘                          │
    │               │                                  │
    │               ▼                                  │
    │      ┌────────────────┐                          │
    │      │ Loop back to 1 │                          │
    │      └────────────────┘                          │
    │                                                  │
    └────────────────────────────────────────────────────┘

    * Phase 2: LLM will actually rewrite the code
      Phase 1: Just logs "mutation triggered"


┌─────────────────────────────────────────────────────────────────────┐
│                        DATA FLOW                                     │
└─────────────────────────────────────────────────────────────────────┘

User Browser
    │
    │ Opens http://yourapp.vercel.app
    ▼
┌─────────────────┐
│  God View       │
│  (React)        │
│                 │
│  useEffect()    │─────┐
│    setInterval  │     │ Every 1 second
│    1000ms       │     │
└─────────────────┘     │
                        │
        ┌───────────────┘
        │
        │ fetch(API_URL + '/status')
        │
        ▼
┌─────────────────────────────┐
│  FastAPI Server             │
│  (Railway/Render)           │
│                             │
│  @app.get("/status")        │
│    return watcher.status    │
└─────────────┬───────────────┘
              │
              │ Read from memory
              ▼
┌─────────────────────────────┐
│  Watcher (Global Instance)  │
│                             │
│  Current state:             │
│  • generation = 5           │
│  • status = "ALIVE"         │
│  • logs = [...]             │
│  • execution_times = [...]  │
└─────────────┬───────────────┘
              │
              │ Updated by background thread
              │
              ▼
┌─────────────────────────────┐
│  Watcher Thread             │
│  (Infinite Loop)            │
│                             │
│  while is_running:          │
│    run_organism()           │
│    update_state()           │
│    sleep(3)                 │
└─────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                        DEPLOYMENT ARCHITECTURE                       │
└─────────────────────────────────────────────────────────────────────┘

Internet
    │
    ├────────────────────────┬────────────────────────┐
    │                        │                        │
    ▼                        ▼                        ▼
┌─────────┐           ┌──────────┐            ┌──────────┐
│  User   │           │  GitHub  │            │   Dev    │
│ Browser │           │   Repo   │            │ Machine  │
└────┬────┘           └─────┬────┘            └────┬─────┘
     │                      │                      │
     │                      │ Push code            │ git push
     │                      │                      │
     │              ┌───────┴───────┐              │
     │              │               │              │
     │              ▼               ▼              │
     │         ┌────────┐      ┌────────┐         │
     │         │ Vercel │      │Railway │         │
     │         │        │      │        │         │
     │         │ Auto   │      │ Auto   │         │
     │         │ Deploy │      │ Deploy │         │
     │         └───┬────┘      └───┬────┘         │
     │             │               │              │
     │    Frontend │               │ Backend      │
     │             │               │              │
     ▼             ▼               ▼              ▼
┌─────────────────────────────────────────────────────┐
│                                                     │
│   https://your-app.vercel.app (Frontend)           │
│         │                                           │
│         │ Fetches data from                         │
│         ▼                                           │
│   https://your-app.railway.app (Backend)           │
│                                                     │
└─────────────────────────────────────────────────────┘

Environment Variables:
┌────────────────────┬──────────────────────────────┐
│  Frontend (Vercel) │  Backend (Railway)           │
├────────────────────┼──────────────────────────────┤
│  NEXT_PUBLIC_API   │  OPENAI_API_KEY (Phase 2)    │
│  _URL = Railway    │  TARGET_LATENCY = 1.0        │
│  backend URL       │  PORT = 8000                 │
└────────────────────┴──────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                        TECHNOLOGIES                                  │
└─────────────────────────────────────────────────────────────────────┘

Backend Stack:
  • Python 3.11         → Runtime
  • FastAPI             → Web framework
  • Uvicorn             → ASGI server
  • Pydantic            → Data validation
  • subprocess          → Process management
  • threading           → Background tasks
  • python-dotenv       → Environment vars
  • OpenAI SDK          → LLM (Phase 2)

Frontend Stack:
  • Next.js 14          → React framework
  • TypeScript          → Type safety
  • Tailwind CSS        → Styling
  • Lucide Icons        → Icon library
  • React Hooks         → State management

DevOps:
  • Docker              → Containerization
  • Railway/Render      → Backend hosting
  • Vercel              → Frontend hosting
  • Git/GitHub          → Version control
  • Bash Scripts        → Automation


┌─────────────────────────────────────────────────────────────────────┐
│                     FILE DEPENDENCY GRAPH                            │
└─────────────────────────────────────────────────────────────────────┘

Backend:
    main.py
       │
       ├── imports watcher.py
       │      │
       │      └── spawns organism.py (subprocess)
       │
       └── serves FastAPI endpoints

Frontend:
    app/layout.tsx
       │
       └── app/page.tsx
              │
              ├── imports React hooks
              ├── fetches from API
              └── uses Tailwind classes

Config:
    .env / .env.local
       │
       ├── backend/.env → main.py
       └── frontend/.env.local → page.tsx
